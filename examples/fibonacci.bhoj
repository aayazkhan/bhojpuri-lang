// Fibonacci: "kaam" se function banaw, "lauta da" se jawab lautaw
ka ho bhaiya
  kaam fib(n) {
    jadi (n < 2) {
      lauta da n;
    }
    lauta da fib(n - 1) + fib(n - 2);
  }

  har i = 0 se 9 tak {
    bol ho `fib({i}) = {fib(i)}`;
  }
chalat bani bhaiya
